doctype html
html
  head
    title= title
    link(rel="stylesheet", href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css")
  body
    .container.mt-5
      h1.mb-4 Pets Information
      // Enlace para añadir/editar un objeto
      a.btn.btn-success.mb-3(href="/update-cobj") Add or Edit Pet
      if records.length
        table.table.table-bordered
          thead
            tr
              th Name
              th Chip Number
              th Breed
          tbody
            each record in records
              tr
                td= record.properties.name
                td= record.properties.chipnumber
                td= record.properties.breed
      else
        p.text-muted No records found.



